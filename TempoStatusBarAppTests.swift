import XCTest
@testable import TempoStatusBarApp

final class TempoStatusBarAppTests: XCTestCase {
    
    func testExample() throws {
        // This is a placeholder test to ensure the test target is properly configured
        XCTAssertTrue(true)
    }
} 